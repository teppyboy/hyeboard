# Copy to .env — sets HYEB_SESSION_SECRET.
# All other config lives in config.json (checked into repo, no secrets).
#
# Generate: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"

HYEB_SESSION_SECRET=
