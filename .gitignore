node_modules/
dist/
.worktrees/
.wrangler/
.turbo/
coverage/
*.tsbuildinfo
apps/web/test-results/
apps/web/playwright-report/
apps/web/blob-report/

# Secrets and local env
.env
.env.*
!.env.example
compose.env
.dev.vars

# HAR captures contain cookies, tokens, session IDs, and PII.
*.har
!samples/har-redacted/*.har

# Scratch credential/notes files used during local testing.
cred.txt
credentials.txt
*.creds.txt

# Local analysis scratch (probe captures, decoded bodies).
.tmp/
.superpowers/
